upstream asa-server {
  server asa-server:80;
}
upstream asa-server02 {
  server asa-server02:80;
}
#upstream asa-server-mercado {
  #server asa-server-mercado:80;
#}
#upstream asa-server-mail {
#  server asa-server-mail:80;
#}
server {
  listen 80 default_server;
  server_name felipe.asa.br www.felipe.asa.br;
  
    #charset koi8-r;
    access_log  /var/log/nginx/access.log  main;
    error_log  /var/log/nginx/error.log  debug;

    location / {
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Url-Scheme $scheme;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header Host $host;
      proxy_redirect off;
      proxy_pass http://asa-server;
    }
} 

server {
  listen 80;
  server_name felipe.felipe.asa.br;
  
    #charset koi8-r;
    access_log  /var/log/nginx/access.log  main;
    error_log  /var/log/nginx/error.log  debug;

    location / {
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Url-Scheme $scheme;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header Host $host;
      proxy_redirect off;
      proxy_pass http://asa-server;
    }
   # location /mercado.felipe.asa.br {
   #   proxy_set_header X-Forwarded-Proto https;
    #  proxy_set_header X-Url-Scheme $scheme;
    #  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #  proxy_set_header Host $host;
    #  proxy_redirect off;
     # proxy_pass http://asa-server-mercado;
   # }
} 

server {
  listen 80;
  server_name teste.felipe.asa.br;
  
    #charset koi8-r;
    access_log  /var/log/nginx/access.log  main;
    error_log  /var/log/nginx/error.log  debug;

    location / {
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Url-Scheme $scheme;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header Host $host;
      proxy_redirect off;
      proxy_pass http://asa-server02;
    }
    #location /mail.felipe.asa.br {
      #proxy_set_header X-Forwarded-Proto https;
      #proxy_set_header X-Url-Scheme $scheme;
      #proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      #proxy_set_header Host $host;
      #proxy_redirect off;
      #proxy_pass http://asa-server-mail;
    #}
} 